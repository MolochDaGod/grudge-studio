apiVersion: v1
kind: ConfigMap
metadata:
  name: grudge-config
  namespace: grudge-warlords
data:
  NODE_ENV: "production"
  PORT: "5000"
  BACKEND_URL: "https://yourdomain.com"  # Change to your actual domain
  LOG_LEVEL: "info"

